{
  "name": "medical-science-site",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/write-firebase-config.js",
    "optimize:images": "node scripts/optimize-images.mjs",
    "deploy:rules": "firebase deploy --only firestore:rules",
    "deploy:rules:npx": "npx firebase-tools deploy --only firestore:rules --project studio9-medical"
  },
  "dependencies": {
    "firebase-admin": "^13.6.0"
  },
  "devDependencies": {
    "sharp": "^0.35.3"
  }
}
